// Requires gradle 7.2

allprojects {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'

    sourceCompatibility = '17'
    targetCompatibility = '17'
}

ext {
    releaseFileName = "${projectName}-${version}.zip"
}